#include<Stdio.h>
#define tam 5000

struct contato{
    char nome[25];
    char telefone[16];
    char email[35];
};

int hash(struct contato c){
    char *b;
    int soma=0;
    int cont=0;

    while(b!="\0"){
        soma += c.nome[cont];
        cont++;
    }
    return soma %
}
